// Quick patch for Upload.tsx rate limiting

// Find line 318 with:
const RATE_LIMIT_SECONDS = 10; // Client-side rate limiting

// Replace with:
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  
  // Different rate limits: 6 seconds for admin, 12 seconds for regular users  
  const RATE_LIMIT_SECONDS = userProfile.role === 'admin' ? 6 : 12;

  // Client-side rate limit
  const lastUpload = localStorage.getItem('lastUploadTime');
  // ... rest of the code stays the same
